<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
      IMPORTANT:
      - DO NOT set Foreground on TabItem (it will INHERIT into tab content).
      - Only color the HEADER presenter via TextElement.Foreground.
      - Keep named brushes INSIDE template so Storyboard.TargetName always resolves.
    -->

    <Style x:Key="PrimaryTabItemStyle"
           TargetType="{x:Type TabItem}">
        <Setter Property="SnapsToDevicePixels"
                Value="True" />
        <Setter Property="Margin"
                Value="8,0,8,0" />
        <Setter Property="Padding"
                Value="22,12" />
        <Setter Property="Background"
                Value="Transparent" />
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="BorderBrush"
                Value="#0078D7" />

        <!-- IMPORTANT: keep Foreground UNSET here to avoid inheriting into tab content -->

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Border x:Name="HeaderBorder"
                            CornerRadius="12"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True"
                            RenderTransformOrigin="0.5,0.5">

                        <!-- scale animation target -->
                        <Border.RenderTransform>
                            <ScaleTransform x:Name="scale"
                                            ScaleX="1"
                                            ScaleY="1" />
                        </Border.RenderTransform>

                        <!-- brushes MUST be named inside this template -->
                        <Border.Background>
                            <SolidColorBrush x:Name="BgBrush"
                                             Color="Transparent" />
                        </Border.Background>

                        <Border.BorderBrush>
                            <SolidColorBrush x:Name="BdBrush"
                                             Color="#0078D7" />
                        </Border.BorderBrush>

                        <Border.BorderThickness>
                            <TemplateBindingExtension Property="BorderThickness" />
                        </Border.BorderThickness>

                        <!-- HEADER ONLY -->
                        <ContentPresenter x:Name="HeaderContent"
                                          ContentSource="Header"
                                          RecognizesAccessKey="True"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          TextElement.Foreground="#333333" />
                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- ===== SELECTED ===== -->
                        <Trigger Property="IsSelected"
                                 Value="True">
                            <Setter TargetName="HeaderContent"
                                    Property="TextElement.Foreground"
                                    Value="White" />

                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BgBrush"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#0078D7"
                                                        Duration="0:0:0.12" />
                                        <ColorAnimation Storyboard.TargetName="BdBrush"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#0078D7"
                                                        Duration="0:0:0.12" />

                                        <!-- tiny pop -->
                                        <DoubleAnimation Storyboard.TargetName="scale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         To="1.02"
                                                         Duration="0:0:0.08" />
                                        <DoubleAnimation Storyboard.TargetName="scale"
                                                         Storyboard.TargetProperty="ScaleY"
                                                         To="1.02"
                                                         Duration="0:0:0.08" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>

                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="scale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         To="1"
                                                         Duration="0:0:0.10" />
                                        <DoubleAnimation Storyboard.TargetName="scale"
                                                         Storyboard.TargetProperty="ScaleY"
                                                         To="1"
                                                         Duration="0:0:0.10" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <!-- ===== HOVER (ONLY WHEN NOT SELECTED) ===== -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver"
                                           Value="True" />
                                <Condition Property="IsSelected"
                                           Value="False" />
                                <Condition Property="IsEnabled"
                                           Value="True" />
                            </MultiTrigger.Conditions>

                            <MultiTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- light blue hover -->
                                        <ColorAnimation Storyboard.TargetName="BgBrush"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#E6F2FF"
                                                        Duration="0:0:0.12" />
                                        <DoubleAnimation Storyboard.TargetName="scale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         To="1.01"
                                                         Duration="0:0:0.10" />
                                        <DoubleAnimation Storyboard.TargetName="scale"
                                                         Storyboard.TargetProperty="ScaleY"
                                                         To="1.01"
                                                         Duration="0:0:0.10" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>

                            <MultiTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BgBrush"
                                                        Storyboard.TargetProperty="Color"
                                                        To="Transparent"
                                                        Duration="0:0:0.12" />
                                        <DoubleAnimation Storyboard.TargetName="scale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         To="1"
                                                         Duration="0:0:0.10" />
                                        <DoubleAnimation Storyboard.TargetName="scale"
                                                         Storyboard.TargetProperty="ScaleY"
                                                         To="1"
                                                         Duration="0:0:0.10" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>

                        <!-- ===== DISABLED ===== -->
                        <Trigger Property="IsEnabled"
                                 Value="False">
                            <Setter TargetName="HeaderContent"
                                    Property="TextElement.Foreground"
                                    Value="#999999" />
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BgBrush"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#F2F2F2"
                                                        Duration="0:0:0.10" />
                                        <ColorAnimation Storyboard.TargetName="BdBrush"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#CCCCCC"
                                                        Duration="0:0:0.10" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

</ResourceDictionary>
